generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model PostLike {
  slug      String   @id
  count     Int      @default(0)
  updatedAt DateTime @updatedAt
}
