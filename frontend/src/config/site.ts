/**
 * Site feature flags and configuration.
 * Toggle features on/off here — the site and nav adapt automatically.
 */
export const siteConfig = {
  /** Show blog page and navigation link */
  showBlog: false,
  /** Show docs page and navigation link */
  showDocs: false,
} as const;
